[package]
name = "smolder-core-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
smolder-core = { package = "smolder-smb-core", path = "../../smolder-core" }
smolder-proto = { path = "../../smolder-proto" }
tokio = { version = "1", features = ["macros", "rt"] }
